#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    
    install_requires=[
        "setuptools-markdown",
        "numpy",
        "scipy",
        "matplotlib",
    ],
    long_description_markdown_filename='README.md',
   
)
